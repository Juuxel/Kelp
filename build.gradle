plugins {
    id 'java'
}

group = 'io.github.juuxel'
version = '1.0.0'

tasks.withType(JavaCompile).configureEach {
    options.release = 25
}

tasks.named('compileJava', JavaCompile) {
    options.javaModuleMainClass = 'juuxel.translationtool.Main'
    options.javaModuleVersion = version
}
